@using Macone.Models.Entities
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Category>
<ul>
    @foreach (var item in Model)
    {
        <li><a class="home-products" data-id="@item.Id">@item.Name</a></li>
    }
</ul>